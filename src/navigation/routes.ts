import BottomNavigation from '../components/BottomNavigation';
import AddTaskOwner from '../components/views/AddTaskOwner';

const appRoutes = {
  Dashboard: {
    screen: BottomNavigation,
  },
  AddTaskOwner: {
    screen: AddTaskOwner,
  },
};

export default appRoutes;
